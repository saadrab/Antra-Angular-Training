import { SortFirstNamePipe } from './sort-first-name.pipe';

describe('SortFirstNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SortFirstNamePipe();
    expect(pipe).toBeTruthy();
  });
});
